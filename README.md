# anon-chat
**AnonChat** is a privacy-first real-time chat platform — no account, no phone, no traces. Built with Node.js and Socket.io, it auto-assigns every user a random identity like `ShadowFox#4821`. Chat in the Global Lobby or create private rooms using a 6-character code. Messages vanish on server restart. Pure anonymous conversation, zero data stored.
